# Anomaly-Detection-Model
ML pipeline for attack detection using a Transformer-VAE for feature extraction. Combines Isolation Forest, One-Class SVM, and LOF scores through an Attention-based fusion layer to optimize F1-performance. Features robust preprocessing, automated threshold tuning, and high-precision results for identity compromise scenarios
